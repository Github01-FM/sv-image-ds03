# This file is used to reserve vendor process when hibernation.
# Usage:
# 1. use `ps` to check the reserved process name when process is running
# 2. add the process name to VENDOR_RESERVE_PROCESS
# 3. separate process names with ':'
# EXAMPLES: VENDOR_RESERVE_PROCESS=com.vendor.app.name1:com.vendor.app.name2:com.vendor.app.name3

VENDOR_RESERVE_PROCESS=
